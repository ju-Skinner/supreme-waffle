import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { html, render } from 'lit-html';

import { extractArgTypes } from '@pxtrn/storybook-addon-docs-stencil';

<Meta title="Example/Atoms/my-component" component="my-component" argTypes={extractArgTypes('my-component')} />

export const Template = (args) => html`<my-component first="${args.first}" middle="${args.middle}" last="${args.last}"> </my-component>`;

# my-component

This is a sample of using MDX with Web Components.

<Canvas>
  <Story name="Primary" args={{ first: 'Aldo', middle: 'Charlie', last: 'Yates' }}>
    {Template.bind()}
  </Story>
</Canvas>
